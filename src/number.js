/**
 * @namespace Number
 */

/**
 * Returns a string representation of a number that does not use exponential notation and has exactly precision digits after the decimal place.
 * @method Number#toFixed
 * @param {Number} precision Amount of digits to use after the decimal place.
 * @returns {String} The string representation of the number with exactly precision digits after the decimal place.
 */

/**
 * Produces a random number between the inclusive lower and upper bounds.
 * @method Number.random
 * @param {Number} min Lower boundary.
 * @param {Number} max Upper boundary.
 * @returns {Number} The random number.
 */
